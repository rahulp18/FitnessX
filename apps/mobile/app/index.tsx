import { View, Text } from "react-native";
import React from "react";

const index = () => {
  return (
    <View className="flex items-center justify-center flex-1">
      <Text className="text-red-500 text-3xl">index</Text>
    </View>
  );
};

export default index;
